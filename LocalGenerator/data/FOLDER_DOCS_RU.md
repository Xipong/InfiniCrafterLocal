# LocalGenerator/data

Compact reference/seed JSON data for generator policies, catalogs, prompts, VFX/effect references and supporting lookups.

Data is not a huge database authority and not executable gameplay by itself. Runtime support still requires Python compiler/validator and C# executor support.
