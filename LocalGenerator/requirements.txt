# Required only for procedural sprite fallback/postprocess.
Pillow>=10.0.0

# No requests dependency: server uses Python stdlib urllib.

# Strict JSON boundaries shared by runtime-plan, gameplay, visual and VFX contracts.
pydantic>=2.13,<3
