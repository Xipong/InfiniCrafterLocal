# contracts/schemas

Сгенерированные JSON Schema для внешних границ:

- runtime plan;
- attack/gameplay wire DTO;
- visual kit;
- VFX manifest.

Обновление: `python tools/export_contract_schemas.py`.
Проверка без изменения файлов: `python tools/export_contract_schemas.py --check`.
