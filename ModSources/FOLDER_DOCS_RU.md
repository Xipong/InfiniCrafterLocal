# ModSources

Здесь лежит активный tModLoader source tree. Для этой версии активный мод: `InfiniCrafterLocal/`.

Не смешивать с build/cache: `.tml-build-cache`, `.nuget`, logs и reports не являются source of truth.
